environment: development

databases:
  postgresql:
    host: localhost
    port: 5432
    password: root
    username: postgres
    database: blog
    ssl_mode: disable
    pool_max_conns: 5

api:
  host:
  port: 8080
